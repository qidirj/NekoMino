# 版本列表

## 内部测试前

格式: `0.0.yyyymmdda '代号'`  
`constants.h` 存储 `0`, `0`, `yymmdd##`

### 0.0.20260519a 'playable'

内核基本完成。尚未编写渲染代码，使用测试初期的纯文本调试代码渲染。

上传版本的默认配置为 20G。目前仅测试了 DAS Gravity IMS。

### 0.0.20260519b 'playable?'

测试并修复了 ARR ARE lARE IRS IHS。

还没有测试 LMost, RMost, ID 三个键。

### 0.0.20260519c 'playable!'

实现了锁定延迟及其刷新，机制见对应文档。

临时渲染代码加入了锁延渲染。

编写了机制文档，并新增了文档目录。

### 0.0.20260521a 'config update but half done'

本次更新主要处理 backstage。

修复了测试主函数的一个致命问题，其导致每一帧都调用了渲染。

实现了配置文件（形式见机制文档）的读取与验证。为此，实现了配置文件数据结构，并重写了所有 enum 以方便地与字符串之间互相转化。

整理了代码和文件结构。
